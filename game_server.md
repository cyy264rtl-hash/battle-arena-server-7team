# 🖥️ 서버 아키텍처 설계 — 협동 미로 탈출 게임

## 1. 전체 구조 개요

이 게임은 **서버 권위(authoritative server) 모델**로 가는 게 맞다. 즉, 클라이언트는 "나 이렇게 움직이고 싶어요"라는 입력만 보내고, 실제로 이동이 됐는지, 코인을 먹었는지, 협동 장치가 작동했는지는 전부 서버가 판단해서 결과를 클라이언트들에게 되돌려주는 방식이다. 이렇게 해야 한 명이 클라이언트를 조작해서 순간이동하거나 코인을 몰래 먹는 식의 치팅을 막을 수 있다.

통신은 실시간으로 계속 주고받아야 하니까 **WebSocket** 같은 상시 연결 방식을 쓴다. HTTP처럼 요청할 때마다 새로 연결하는 방식은 이동처럼 초당 여러 번 일어나는 이벤트에는 맞지 않는다.

## 2. 서버 동작 흐름 (큰 단계)

1. **접속 단계**: 클라이언트가 서버에 연결 → 인증 → 대기 상태
2. **방(Room) 단계**: 파티 생성/참가 → 인원 체크(2~4인) → 방장이 시작 누르면 게임 세션 생성
3. **게임 시작**: 미로 생성, 코인 배치, 인원수만큼 스폰 위치 지정 → 모든 클라이언트에 초기 상태 전송
4. **게임 루프**: 게임이 끝날 때까지 계속 도는 실시간 루프 (3번 항목에서 상세히 다룸)
5. **게임 종료**: 결과 집계 → 클라이언트에 결과 전송 → 방 정리(메모리 해제)

## 3. 게임 루프 — 실제로 서버가 매 순간 하는 일

서버는 이 게임을 "이벤트가 올 때만 반응"하는 게 아니라, **일정한 주기(tick)**로 계속 상태를 갱신하는 구조로 짜는 게 좋다. 보통 초당 20~30틱 정도면 충분하다. 매 틱마다 서버는 다음 순서로 일을 처리한다.

1. **입력 수집**: 그 틱 동안 들어온 모든 플레이어의 입력(이동 방향, 키 입력 등)을 큐에서 꺼냄
2. **검증**: 벽에 부딪히진 않는지, 이동 속도가 비정상적이지 않은지 체크
3. **상태 갱신**: 캐릭터 위치, 코인 상태, 협동 장치 위치 등 서버가 들고 있는 "진짜 게임 상태"를 업데이트
4. **판정 처리**: 이번 틱에 코인을 먹었는지, 공동 코인 조건이 충족됐는지, 협동 장치가 목적지에 도달했는지 등을 체크
5. **브로드캐스트**: 갱신된 상태 중 각 플레이어에게 필요한 부분만(시야 제한 반영해서) 전송

이 루프가 게임이 끝날 때까지 반복된다. `GAME_SPEC.md`의 "게임에서 벌어지는 일" 표에 있는 각 행은 사실 이 루프 안에서 4번(판정 처리) 단계에 걸리는 개별 이벤트들이라고 보면 된다.

## 4. 서버가 들고 있어야 할 상태 (데이터 구조 예시)

| 단위 | 서버가 저장해야 할 값 |
|---|---|
| Room | 방 ID, 참가자 목록, 미로 시드, 게임 상태(대기/진행/종료), 남은 시간 |
| Player | 플레이어 ID, 현재 위치, 점수, 시야에 들어온 탐색 구역, 접속 상태(온라인/끊김) |
| Coin | 코인 ID, 위치, 종류(개인/공동), 획득 여부 |
| CoopDevice(협동 장치) | 장치 ID, 현재 탑승 인원, 인원별 배정 키, 현재 위치/방향, 상태(대기/작동중/성공/실패) |

## 5. 협동 장치(WASD 분할 조작)의 서버 로직 — 상태 머신

가장 로직이 복잡한 부분이라 별도의 상태 흐름으로 정리한다.

- **대기(IDLE)**: 아무도 안 탄 상태
- **탑승 감지(BOARDING)**: 플레이어가 장치 범위에 들어오면 인원 카운트 → 인원수에 따라 W/A/S/D를 자동 배정 → 배정 정보를 탑승자 전원에게 전송
- **작동중(ACTIVE)**: 각 플레이어의 키 입력을 서버가 실시간으로 수신 → 배정된 키들을 조합해서 "이 틱에 장치가 어느 방향으로 얼마나 움직였는지" 계산 → 위치 갱신 후 브로드캐스트
- **이탈 감지**: 작동 중 누군가 범위를 벗어나면 → 인원 재계산 → 키 재배정하거나 장치를 잠시 멈춤(PAUSED)
- **성공(SUCCESS)**: 목적지 도달 시 → 문 개방/보상 처리 → 장치를 IDLE로 리셋, 탑승자는 개별 이동 모드로 복귀
- **실패(FAIL)**: 벽 충돌 등 실패 조건 시 → 장치 위치를 시작점으로 리셋, 탑승자에게 실패 알림 → 다시 시도 가능하게 BOARDING으로 복귀

이렇게 상태 머신으로 짜두면, "지금 이 장치가 어떤 상태인지"에 따라 서버가 어떤 입력을 받아들일지가 명확해져서 구현할 때 버그가 훨씬 줄어든다.

## 6. 놓치기 쉬운 서버 고려사항

- **지연(latency) 보정**: 4명이 각각 다른 네트워크 상태로 접속하니, 입력이 서버에 도착하는 시점이 다 다르다. 그대로 처리하면 "먼저 누른 사람이 유리해지는" 불공평이 생길 수 있어서, 같은 틱 안에 들어온 입력은 동시에 처리하는 식으로 맞춰야 한다.
- **재접속 시 상태 복구**: 끊긴 플레이어가 협동 장치에 타고 있던 도중이었다면, 재접속했을 때 그 장치가 지금 어떤 상태인지(작동중/일시정지)까지 같이 복원해줘야 한다.
- **동시 처리 충돌**: 코인 습득이나 문 개방처럼 "누가 먼저"가 중요한 이벤트는 같은 틱에 여러 요청이 몰려도 서버가 하나의 처리 순서를 정해서 순차적으로 처리해야 데이터가 꼬이지 않는다.
